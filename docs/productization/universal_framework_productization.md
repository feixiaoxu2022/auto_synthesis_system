# Universal Framework 产品化方案
## 结合 Harness机制 + Bloom设计思路

基于文章: https://www.anthropic.com/engineering/effective-harnesses-for-long-running-agents

---

## 🎯 核心理念: Init-Execute 模式

### Anthropic Harness机制的核心

```python
# 传统Agent执行
def traditional_agent(task):
    while not done:
        action = agent.think_and_act(task, history)  # 每次都要重新理解
        result = execute(action)
        history.append(result)
    return final_result

# Harness机制: Init + Execute分离
def harness_agent(task):
    # 1. Init阶段: 深度理解和规划
    plan = agent.init_and_plan(task)  # 一次性深度理解
    
    # 2. Execute阶段: 轻量执行
    for step in plan.steps:
        result = agent.execute_step(step)  # 只执行,不重复理解
        if need_replan:
            plan = agent.replan(plan, result)
    
    return final_result
```

**关键优势**:
- ✅ Init阶段可以用强模型深度思考
- ✅ Execute阶段可以用弱模型快速执行
- ✅ 减少重复理解,提高效率
- ✅ 计划可持久化,可恢复执行

---

## 💡 结合Universal Framework的产品化架构

### 当前问题分析

**Universal Framework现状**:
```
scenario/
├── tools/                 # 需要手写Python代码
├── db/                    # 需要设计数据库schema
├── samples/               # 需要手动配置
└── checkers/              # 需要写验证逻辑

问题:
  × 开发成本高 - 每个场景都要写大量代码
  × 技术门槛高 - 需要懂Python、SQLite、业务逻辑
  × 迭代周期长 - 改一个规则要改多处代码
```

### 新架构: Agent自动生成 + Harness执行

```
┌─────────────────────────────────────────────────────────┐
│           Universal Framework Agent Platform            │
├─────────────────────────────────────────────────────────┤
│                                                           │
│  Phase 1: Init (类似Bloom Ideation + Anthropic Init)    │
│  ────────────────────────────────────────────────       │
│  输入: 业务场景描述(自然语言)                           │
│    ↓                                                     │
│  Agent深度理解和规划:                                    │
│    1. 理解业务规则                                       │
│    2. 生成Tools定义                                      │
│    3. 设计DB Schema                                      │
│    4. 生成Sample配置                                     │
│    5. 生成Checker逻辑                                    │
│    6. 制定执行计划                                       │
│    ↓                                                     │
│  输出: Execution Plan (可持久化)                         │
│                                                           │
├─────────────────────────────────────────────────────────┤
│                                                           │
│  Phase 2: Execute (类似Universal Framework Rollout)     │
│  ────────────────────────────────────────────────       │
│  输入: Execution Plan                                    │
│    ↓                                                     │
│  自动化执行:                                             │
│    1. 根据Plan生成Python代码                             │
│    2. 初始化DB和数据                                     │
│    3. 运行评测                                           │
│    4. 执行Checker验证                                    │
│    5. 生成结果报告                                       │
│    ↓                                                     │
│  输出: 高质量评测样本 + 评测报告                         │
│                                                           │
└─────────────────────────────────────────────────────────┘
```

---

## 🏗️ 详细设计

### 1. Init Phase: 智能场景理解和规划

#### 输入: 自然语言业务描述

```yaml
# user_input.yaml
scenario_description: |
  我要测试Agent的请假管理能力。
  用户可以查询请假余额、提交请假申请、修改请假申请。
  业务规则:
  - 员工年假12天,病假10天
  - 请假必须提前3天申请
  - 余额不足时不能提交
  - 请假需要经理审批
  
test_goals:
  - 测试Agent是否正确查询余额
  - 测试余额不足时的处理
  - 测试请假流程完整性
```

#### Agent自动生成的Plan

```python
# execution_plan.json
{
  "scenario_name": "leave_application",
  "understanding": {
    "business_domain": "HR - Leave Management",
    "core_capabilities": [
      "查询余额",
      "提交请假",
      "处理余额不足"
    ],
    "complexity": "medium"
  },
  
  "tools_design": [
    {
      "name": "query_leave_balance",
      "description": "查询员工请假余额",
      "parameters": ["employee_id"],
      "returns": {"annual_leave": "float", "sick_leave": "float"},
      "implementation": "SELECT annual_leave, sick_leave FROM employees WHERE id=?"
    },
    {
      "name": "create_leave_application",
      "description": "创建请假申请",
      "parameters": ["employee_id", "leave_type", "start_date", "days"],
      "returns": {"application_id": "str", "status": "str"},
      "implementation": "INSERT INTO leave_applications ...",
      "validation": "check_balance_sufficient AND check_advance_days >= 3"
    }
  ],
  
  "db_schema": {
    "employees": {
      "id": "TEXT PRIMARY KEY",
      "annual_leave": "REAL",
      "sick_leave": "REAL"
    },
    "leave_applications": {
      "id": "TEXT PRIMARY KEY",
      "employee_id": "TEXT",
      "leave_type": "TEXT",
      "days": "REAL",
      "status": "TEXT"
    }
  },
  
  "samples": [
    {
      "id": "LA_001",
      "description": "余额充足,正常请假",
      "initial_state": {
        "employees": [{"id": "E001", "annual_leave": 12.0}]
      },
      "user_simulator_prompt": "你想请3天年假",
      "expected_outcome": {
        "success": true,
        "checklist": {
          "application_created": true,
          "balance_deducted": true
        }
      }
    },
    {
      "id": "LA_002", 
      "description": "余额不足,应该拒绝",
      "initial_state": {
        "employees": [{"id": "E001", "annual_leave": 2.0}]
      },
      "user_simulator_prompt": "你想请5天年假",
      "expected_outcome": {
        "success": false,
        "reason": "insufficient_balance"
      }
    }
  ],
  
  "execution_steps": [
    "1. 生成tools代码 (tools/leave_tools.py)",
    "2. 初始化数据库 (db/init_leave_db.py)",
    "3. 生成samples (samples/LA_001.json, LA_002.json)",
    "4. 生成checker (checkers/leave_checker.py)",
    "5. 运行评测 (run_evaluation.py)",
    "6. 生成报告 (reports/leave_evaluation.json)"
  ]
}
```

---

### 2. Execute Phase: 自动化代码生成和执行

#### 代码生成引擎

```python
class CodeGenerator:
    """根据Plan自动生成Universal Framework代码"""
    
    def generate_tools(self, tools_design):
        """生成tools/xxx_tools.py"""
        template = """
from typing import Dict, Any
import sqlite3

class {tool_class_name}:
    def __init__(self, db_path: str):
        self.db_path = db_path
    
    {tool_methods}
"""
        # 根据tools_design填充template
        return generated_code
    
    def generate_db_init(self, db_schema):
        """生成db/init_xxx.py"""
        # 自动生成CREATE TABLE语句
        pass
    
    def generate_samples(self, samples_config):
        """生成samples/xxx.json"""
        # 根据配置生成完整sample文件
        pass
    
    def generate_checker(self, checklist):
        """生成checkers/xxx_checker.py"""
        # 自动生成验证逻辑
        pass
```

#### 执行引擎

```python
class HarnessExecutor:
    """Harness风格的执行引擎"""
    
    def __init__(self, execution_plan):
        self.plan = execution_plan
        self.state = ExecutionState()
    
    def execute(self):
        """按Plan执行,支持断点恢复"""
        
        # 1. 恢复之前的状态(如果有)
        if self.plan.resume_from:
            self.state = load_state(self.plan.resume_from)
        
        # 2. 按步骤执行
        for step in self.plan.execution_steps:
            if step.completed:
                continue  # 跳过已完成的步骤
            
            try:
                result = self._execute_step(step)
                self.state.mark_completed(step, result)
                self.state.save()  # 持久化状态
            except Exception as e:
                self.state.mark_failed(step, e)
                self.state.save()
                
                # 询问是否replan
                if should_replan(e):
                    new_plan = self._replan(step, e)
                    self.plan = new_plan
        
        return self.state.final_result()
    
    def _execute_step(self, step):
        """执行单个步骤"""
        if step.type == "generate_code":
            return self.code_generator.generate(step.spec)
        elif step.type == "run_evaluation":
            return self.evaluator.run(step.samples)
        # ...
    
    def _replan(self, failed_step, error):
        """失败时重新规划"""
        replan_prompt = f"""
        执行步骤 {failed_step} 失败: {error}
        
        原始计划:
        {self.plan}
        
        请分析失败原因并生成新的执行计划。
        """
        
        new_plan = agent.replan(replan_prompt)
        return new_plan
```

---

## 🎨 产品化界面设计

### 用户体验流程

```
┌────────────────────────────────────────────────────┐
│  Step 1: 描述场景 (自然语言)                      │
├────────────────────────────────────────────────────┤
│                                                     │
│  [文本框]                                          │
│  我想测试Agent处理请假申请的能力。                 │
│  业务规则:                                         │
│  - 员工有年假和病假额度                           │
│  - 请假需要检查余额                               │
│  - 余额不足应该拒绝                               │
│                                                     │
│  [高级选项]                                        │
│  ☑ 生成边界case                                   │
│  ☑ 包含错误处理测试                              │
│  ☐ 生成压力测试样本                              │
│                                                     │
│              [下一步: 生成计划] →                 │
└────────────────────────────────────────────────────┘

                      ↓ Agent Init Phase

┌────────────────────────────────────────────────────┐
│  Step 2: 审核执行计划                              │
├────────────────────────────────────────────────────┤
│                                                     │
│  场景理解: ✓                                       │
│    领域: HR - 请假管理                             │
│    复杂度: 中等                                    │
│                                                     │
│  生成的工具: 3个                                   │
│    • query_leave_balance                           │
│    • create_leave_application                      │
│    • update_leave_balance                          │
│                                                     │
│  数据库设计: 2张表                                 │
│    • employees (3个字段)                          │
│    • leave_applications (6个字段)                 │
│                                                     │
│  测试样本: 5个                                     │
│    • LA_001: 正常请假 ✓                           │
│    • LA_002: 余额不足 ✓                           │
│    • LA_003: 边界值测试 ✓                         │
│    • LA_004: 并发请假 ⚠ (可能需要调整)           │
│    • LA_005: 请假撤回 ✓                           │
│                                                     │
│  [编辑计划] [重新生成]  [开始执行] →              │
└────────────────────────────────────────────────────┘

                      ↓ Execute Phase

┌────────────────────────────────────────────────────┐
│  Step 3: 执行进度 (实时更新)                      │
├────────────────────────────────────────────────────┤
│                                                     │
│  [████████████░░░░░░░░] 60%                       │
│                                                     │
│  ✓ 1. 生成工具代码 (完成)                         │
│  ✓ 2. 初始化数据库 (完成)                         │
│  ✓ 3. 生成测试样本 (完成)                         │
│  → 4. 运行评测 (进行中: 3/5 样本)                 │
│     • LA_001: ✓ 通过                              │
│     • LA_002: ✓ 通过                              │
│     • LA_003: 🔄 运行中...                        │
│  ⏸ 5. 生成报告 (等待)                             │
│                                                     │
│  [查看日志] [暂停] [查看中间结果]                 │
└────────────────────────────────────────────────────┘

                      ↓ 完成

┌────────────────────────────────────────────────────┐
│  Step 4: 评测报告                                  │
├────────────────────────────────────────────────────┤
│                                                     │
│  总体结果: 4/5 通过 (80%)                         │
│                                                     │
│  [详情]                                            │
│  ├─ LA_001: ✓ 通过                                │
│  ├─ LA_002: ✓ 通过                                │
│  ├─ LA_003: ✓ 通过                                │
│  ├─ LA_004: ✗ 失败                                │
│  │    失败原因: Agent未正确处理并发冲突          │
│  │    根因分析: 缺少事务处理逻辑                 │
│  │    [查看详情] [重新测试]                      │
│  └─ LA_005: ✓ 通过                                │
│                                                     │
│  [导出报告] [优化样本] [测试其他模型]            │
└────────────────────────────────────────────────────┘
```

---

## 🔑 关键创新点

### 1. 降低开发门槛

**之前**: 需要写大量Python代码
```python
# 用户需要手写
class LeaveTools:
    def query_balance(self, employee_id):
        conn = sqlite3.connect(self.db_path)
        cursor = conn.execute(...)
        # 20行代码...
```

**现在**: 自然语言描述即可
```yaml
工具: 查询请假余额
参数: 员工ID
返回: 年假余额、病假余额
```

### 2. 借鉴Bloom的快速迭代

**Bloom的优势**: 5分钟生成10个场景

**我们的改进**:
- Init阶段用强模型深度规划(10分钟)
- Execute阶段自动生成代码(5分钟)
- 总计15分钟,但质量是Bloom的10倍!

### 3. 结合Harness的可恢复执行

**传统问题**: 执行到一半失败,要从头来

**Harness方案**:
```python
# 执行失败后
state = load_state("execution_xyz_checkpoint.json")
# 从失败的步骤继续
executor.resume_from(state)
```

### 4. 分离Plan和Execute的好处

**Plan可复用**:
```python
# 一次Plan,多次Execute
plan = agent.init("请假管理场景")

# 测试Claude
execute(plan, target_model="claude-sonnet-4")

# 测试GPT
execute(plan, target_model="gpt-4o")

# 测试自己的模型
execute(plan, target_model="my-custom-model")
```

---

## 🚀 实施路线图

### Phase 1: MVP (2周)

**目标**: 验证Init-Execute分离的可行性

**功能**:
- ✅ 自然语言描述 → Execution Plan
- ✅ 自动生成简单tools代码
- ✅ 自动生成DB schema
- ✅ 执行单个sample

**技术栈**:
- Claude Opus 4.5 (Init Phase规划)
- Claude Haiku 4.5 (代码生成)
- 现有Universal Framework (Execute底层)

### Phase 2: 增强 (1个月)

**功能**:
- ✅ 复杂业务规则理解
- ✅ 自动生成完整checker
- ✅ 批量sample生成和执行
- ✅ 执行状态持久化和恢复

### Phase 3: 产品化 (2个月)

**功能**:
- ✅ Web UI界面
- ✅ 实时执行进度展示
- ✅ 报告可视化
- ✅ Plan模板库
- ✅ 多模型对比测试

---

## 💰 价值提升

### 对比现状

| 维度 | 当前手工开发 | Agent自动化 | 提升 |
|------|-------------|------------|------|
| **开发时间** | 2-3天/场景 | 15分钟/场景 | 200x |
| **技术门槛** | 需要Python专家 | 自然语言描述 | 门槛↓90% |
| **质量** | 依赖人工经验 | Agent深度规划 | 一致性↑ |
| **迭代速度** | 改代码需半天 | 改描述5分钟 | 50x |
| **可维护性** | 代码分散难维护 | Plan集中管理 | ↑↑ |

### ROI计算

**假设**:
- 当前: 1个场景2天,1个月开发15个场景
- Agent化: 1个场景15分钟,1天开发30个场景

**效率提升**: 60x
**人力成本**: 从3人团队 → 1人即可

